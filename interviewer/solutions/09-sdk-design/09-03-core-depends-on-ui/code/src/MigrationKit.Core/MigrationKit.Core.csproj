<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Platform-neutral: WPF, MAUI (Windows/Android) and console hosts can all reference this.
         netstandard2.0 would be the next step if .NET Framework hosts must be supported (see 09-04). -->
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>MigrationKit.Core</RootNamespace>
  </PropertyGroup>
</Project>
