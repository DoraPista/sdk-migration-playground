<UserControl x:Class="MigrationKit.Gallery.GalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <DataTemplate x:Key="TileTemplate">
            <StackPanel Width="176" Margin="8">
                <Border Background="#EEEEEE" Height="120">
                    <Image Source="{Binding Thumbnail}" Width="160" Stretch="Uniform" />
                </Border>
                <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" Margin="0,4,0,0" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <!--
        No outer ScrollViewer: it measures its child with infinite height, so the list would build
        every tile. The ListBox brings its own (virtualizing) ScrollViewer.
    -->
    <ListBox ItemsSource="{Binding Items}"
             ItemTemplate="{StaticResource TileTemplate}"
             x:Name="Tiles"
             ScrollViewer.CanContentScroll="True"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
             VirtualizingPanel.IsVirtualizing="True"
             VirtualizingPanel.VirtualizationMode="Recycling"
             VirtualizingPanel.ScrollUnit="Pixel">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <VirtualizingStackPanel Orientation="Vertical" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
    </ListBox>
</UserControl>
