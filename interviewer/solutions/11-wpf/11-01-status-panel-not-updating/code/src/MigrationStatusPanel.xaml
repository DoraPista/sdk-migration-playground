<UserControl x:Class="MigrationKit.Wpf.MigrationStatusPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MigrationKit.Wpf"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MigrationStatusViewModel}">
    <!--
        The DataContext is supplied by the code that creates this panel (one view model per migration).
        A <UserControl.DataContext> element here would REPLACE it at InitializeComponent time; the
        designer-time context (d:DataContext) gives IntelliSense without affecting runtime.
    -->
    <StackPanel Margin="12">
        <TextBlock x:Name="StatusText" FontWeight="Bold" Text="{Binding StatusText}" />
        <TextBlock x:Name="FilesText" Margin="0,6,0,0" Text="{Binding FilesUploaded}" />
        <ProgressBar x:Name="ProgressBar" Height="16" Margin="0,6,0,0" Minimum="0" Maximum="100"
                     Value="{Binding Percent, Mode=OneWay}" />
    </StackPanel>
</UserControl>
