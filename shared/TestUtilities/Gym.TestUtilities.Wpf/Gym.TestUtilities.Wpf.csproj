<Project Sdk="Microsoft.NET.Sdk">
  <!-- Helpers for testing WPF code on an STA thread with a real Dispatcher (Windows only). -->
  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Gym.TestUtilities\Gym.TestUtilities.csproj" />
  </ItemGroup>
</Project>
